#include "Bureaucrat.hpp"

class Form 
{
    private:
        std :: const string name;
        bool signed;
        bool beSigned(Bureaucrat *un_bureaucrat);
        unsigned int grade_signed;
        unsigned int grade_exec;
    public: 


}